/**
 * Sync products from Payload to Stripe
 * Run: pnpm stripe:sync
 *
 * This script is a placeholder — it will be functional once
 * Stripe keys are configured and the app runs in Next.js context.
 * Use /api/stripe-sync POST route instead for now.
 */

console.log("Stripe sync: use POST /api/stripe-sync when Stripe keys are configured.");
console.log("This requires the Next.js server to be running.");
process.exit(0);
